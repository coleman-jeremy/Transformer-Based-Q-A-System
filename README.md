# Transformer-Based Q&A System

Semantic retrieval system comparing BM25 (keyword-based), Word2Vec (static embeddings), and Transformer-based (contextual) retrieval methods, built as part of a Udacity Deep Learning Nanodegree project.

See `project/starter/README.md` for full project setup and details.
